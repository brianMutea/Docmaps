{
  "name": "@docmaps/config",
  "version": "0.1.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "build": "echo 'Config package does not require build step - TypeScript is compiled by consuming apps'",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0"
  }
}
