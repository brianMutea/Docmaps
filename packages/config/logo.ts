/**
 * Centralized Logo Configuration
 * 
 * To use a custom logo image/SVG across the entire application:
 * 1. Place your logo file in the public folder of both apps (web and editor)
 * 2. Update the LOGO_IMAGE_PATH below with the path to your logo
 * 3. The logo will automatically update everywhere in the app
 * 
 * Example:
 * export const LOGO_IMAGE_PATH = '/logo.svg';
 * 
 * To use the default text logo, set LOGO_IMAGE_PATH to null
 */

export const LOGO_IMAGE_PATH: string | null = '/D_logo_light.png'; // Change this to your logo path, e.g., '/logo.svg'
export const LOGO_ALT_TEXT = 'DocMaps Logo';
